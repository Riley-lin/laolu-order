# 🚨 老滷仙線上點餐・危機處理手冊

> 建立：2026-07-28（起因：LINE 後台當天故障，訂單送出但老闆收不到通知）
> 維護：Riley ／ 這份文件不會被部署到網站（`.assetsignore` 已排除 `*.md`）

---

# 📱 PART 1 ─ 給老闆看的（白話版）

## 🔍 遇到怪怪的，先做這件事：看「是誰壞了」

打開手機書籤三個網頁，**一眼就知道問題在誰身上**：

| 網頁 | 管什麼 |
|---|---|
| `status.supabase.com` | 訂單資料庫 |
| `cloudflarestatus.com` | 點餐網頁 |
| LINE Developers Status | LINE 通知 |

**全部綠色** → 是我們自己的問題，找 Riley
**有紅色/黃色** → 是那家公司壞了，**等它修好就好，不要亂動**

> ⚠️ 這一步不做，會在別人家的問題上白忙一整晚。

---

## 🆘 三種最常見的狀況

### 狀況 A：客人說「點不了餐」

1. 自己用 LINE 點一次看看
2. 點得了 → 是那位客人的手機問題（請他把 LINE 完全關掉重開）
3. 點不了 → 看上面三個狀態頁 → 貼公告（見下）

### 狀況 B：老闆收不到新訂單通知（← 07-28 真的發生過）

1. 先看**店務台**還有沒有新單進來
   - **有單** → 只是「通知」壞了，訂單是好的 → 改成盯著店務台看
   - **沒單** → 可能真的沒人點，或系統有事 → 找 Riley
2. 這種狀況**不要關店**，店務台就是最可靠的那一份

### 狀況 C：LINE 整個掛掉，客人進不來

1. **第一備案**：請 Riley 開「應急模式」，用專屬網址讓客人下單
2. **第二備案**（自己就能做）：在**店務台的公告欄**打字：
   ```
   LINE 系統異常，請直接來電訂購 📞0939-955-888
   ```
   打完就會出現在點餐頁最上面

---

## ✅ 每天兩個 30 秒的習慣（比任何程式都有效）

### ① 開店前：自己下一張測試單

```
① 用自己的 LINE 點一樣最便宜的
② 確認：聊天室有出現訊息 → 手機有跳新單通知
③ 按「接單」→ 再按「完成」→ 最後標記取消
```

**為什麼要做**：系統壞掉最可怕的不是壞，是**壞了沒人發現**。
07-28 那次就是客人都下單了，老闆才發現沒收到通知。
每天測一次，最多只會漏掉一天。

### ② 收店後：匯出當天訂單

```
店務台 → 日報 → ⬇️ CSV 匯出 → 存手機
```

**為什麼要做**：所有訂單資料只存在一個地方（雲端資料庫）。
匯出一份，等於幫自己的帳留一份「跟平台無關」的副本。

---

# 🔧 PART 2 ─ 給 Riley 看的（技術版）

## 系統長什麼樣：三個單點故障

```
客人手機
   │
   ├─ LINE 平台 ─────── 入口＋通知      🔴 單點
   │
   ├─ Cloudflare Pages ─ 點餐頁＋看單台  🔴 單點
   │
   └─ Supabase ───────── 資料庫＋Edge Functions  🔴 單點
```

**任何一個掛掉，生意就停。** 這不是做錯，是所有小型系統的起點。
重點是知道它在哪、掛了怎麼辦 —— 一天 10 單的規模，不需要做高可用架構，
**做「判斷得快 ＋ 降級得順」就夠**。

## 各故障的判斷與處理

| 故障 | 怎麼判斷 | 處理 | 客人端表現 |
|---|---|---|---|
| **LINE 平台** | LINE Status 頁；老闆收不到通知但店務台有單 | 開應急模式 or 貼公告 | 進不了點餐頁 |
| **Cloudflare** | `cloudflarestatus.com`；`curl` 打網址看回應 | 只能等，貼實體公告 | 網頁全開不了 |
| **Supabase** | `status.supabase.com`；點餐頁菜單載不出來 | 只能等 | 送不出訂單 |
| **我們自己的 bug** | 三個狀態頁全綠，但功能異常 | 看 Edge Function Logs；必要時回滾部署 | 視情況 |

### 🔑 判斷的第一原則
**先確認「是不是外面壞了」，再看自己的程式碼。**
順序反了，會花整晚 debug 一個不是自己造成的問題。

### 回滾（改壞了怎麼救）
```
Cloudflare Pages → 專案 → Deployments → 選前一版 → Rollback
Edge Function    → 沒有自動回滾 → 用 git 取回舊版重貼
```
👉 所以 **push GitHub 很重要**，那是唯一能回頭的路。

## 老闆的最後手段：資料本體在哪

```
看單台打不開 ≠ 訂單不見
Supabase 後台 → Table Editor → orders → 今天的單全在
```
看單台只是一扇窗，窗破了還有門。**Riley 的手機要能登入 Supabase。**

## 金鑰外洩（07-27 真的發生過）

```
症狀：機密檔案被部署到公開網站
根因：.gitignore 只管 git，wrangler 部署【不看】它
處理：① 檔案移出部署資料夾 ② .assetsignore 補防線
      ③ 重新部署 ④ 【所有鑰匙輪替】← 這步不能省
```
📌 金鑰一律放 `D:\laolu-secrets\`，永遠不要放回專案資料夾。

## 應急模式（LINE 掛掉時）

**設計原則：不複製第二個網站**（兩份程式碼遲早不同步），改用開關。

```
① 開關   店務台 →【🚨 啟動應急模式】（平常關著）
② 通行碼 laolusian.pages.dev/?e=<碼>（店務台可一鍵換新碼）
③ 行為   不強制傳 LINE 訊息／頁面上出現「查詢訂單」／訂單標記 ⚡ 應急單
```

⚠️ 誠實說明：帶碼的網址被轉發，對方也能開（網址本身就是鑰匙）。
三道限制讓它「夠安全」：平常開關是關的 ＋ 隨時可換碼 ＋ 只開幾小時。

---

# 💬 PART 3 ─ 對客人怎麼講（話術）

| 狀況 | 這樣說 |
|---|---|
| 系統暫時不能用 | 「不好意思，線上系統維護中，可以直接打 0939-955-888 幫您記單喔 🙏」 |
| 客人說沒收到通知 | 「幫您確認一下…（看店務台）您的單我們有收到，取餐時間是 X:XX 喔」 |
| 客人下單失敗兩次 | 「不好意思系統怪怪的，我直接幫您記，請問要什麼？」← **不要讓客人一直重試** |
| 應急模式期間 | 「LINE 系統今天有狀況，用這個連結一樣可以點喔 👉（帶碼網址）」 |

**共同原則**：先解決客人的需求（讓他吃到），再處理系統問題。
**永遠不要對客人說「系統壞了我也不知道」** —— 給他一條可以走的路。

---

# 📋 待辦（勾完就是完整防護）

- [ ] 三個狀態頁加進手機書籤（老闆＋Riley）
- [ ] Riley 手機能登入 Supabase
- [ ] 老闆養成「開店前測試單」習慣
- [ ] 老闆養成「收店後匯出 CSV」習慣
- [ ] 🔨 應急模式（開關＋通行碼＋查詢入口）── 待施工
- [ ] 老闆手冊補一節「公告欄怎麼用」
