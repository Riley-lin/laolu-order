# 🔔 通知可靠性部署卡（2026-07-28 夜間備好，隔天執行）

> 起因：#015 訂單成立但老闆沒收到通知（Serverless 冷啟動失敗，line-push 沒被叫醒）
> 詳細追查過程見 `事故報告_20260728_通知漏發.md`

---

## 🔕 提示音已取消（2026-07-29 老闆裁示：「不用，我聽 LINE 的」）

做過但整組移除了，理由記在這裡免得以後有人再提：
- 網頁提示音有**天生上限**：頁面關掉或手機鎖屏，JavaScript 就被暫停 → 等於老闆得**全程開著店務台**
- 就算做到極限（方波和聲＋壓縮器＋語音播報＋持續呼叫 30 秒＋閃爍橫幅）也繞不過這一點
- **#015 的真正解藥是下面的「保底重推」**——那是在伺服器上跑的，老闆手機開著關著都有效

👉 所以這份部署卡剩下的三步，**每一步都是主力，不是加分項**。

---

## 📋 今天要做的三步（約 20 分鐘）

### 步驟 1️⃣ 跑 SQL（建重推＋紀錄＋保溫）

```
Supabase → SQL Editor → 貼上 setup-push-reliability.sql 整份 → Run
```

**它會做**：
- `orders` 加 `push_retry_count` 欄位
- 建 `push_log` 表（每次推播都留紀錄）
- 建兩支函式：`retry_unnotified_orders()`、`warm_line_push()`
- 掛兩個排程：每分鐘重推、每 5 分鐘保溫

**跑完最下面會自動體檢**，應該看到：
```
laolu-retry-push   * * * * *    active
laolu-warm-push    */5 * * * *  active
push_retry_count   integer
push_log筆數        0
```

> ⚠️ 如果 `create extension pg_cron` 報錯：
> 改到 **Dashboard → Database → Extensions**，搜尋 `pg_cron` 手動啟用，再重跑整份。

---

### 步驟 2️⃣ 部署 `line-push`

```
Supabase → Edge Functions → line-push → 貼上 supabase/functions/line-push/index.ts → Deploy
```

**這次改了什麼**：
- ✅ 新增 `ping` 保溫入口（收到就回 OK，讓它不睡）
- ✅ 每次推播都寫進 `push_log`（成功、失敗、LINE 回什麼狀態碼）
- ✅ 補發的卡片標題變「🔁 補發通知 #XXX」橘色（老闆一眼分辨不是新單）
- ✅ 卡片底部文案修正：客人推播已關，不能再寫「客人自動收到取餐時間」

---

### 步驟 3️⃣ 部署 `line-webhook`

```
Supabase → Edge Functions → line-webhook → 貼上 supabase/functions/line-webhook/index.ts → Deploy
```

**這次改了什麼**（昨天談定但還沒部署）：
- ✅ 送單確認改**三重驗證**（同一人 × 同一單 × 同一支電話，全中才回）
- ✅ 移除純數字綁定（那是資安漏洞：亂打數字能綁走別人的訂單）
- ✅ 關閉公休／營業時間自動回覆（關鍵字比對不懂上下文，會答非所問）
- ✅ 回覆去 emoji、「老闆確認接單後」→「店家確認接單後」

---

## 🧪 驗收（做完跑一次）

### A. 保溫有沒有在跑
等 5 分鐘後查：
```sql
select id, status_code, created at time zone 'Asia/Taipei' as 台北時間
from net._http_response order by created desc limit 10;
```
應該**每 5 分鐘一筆 200**。

### B. 推播紀錄有沒有寫進去
```sql
select order_no, kind, ok, status_code, created_at at time zone 'Asia/Taipei' as 台北時間
from public.push_log order by created_at desc limit 20;
```
下一張測試單之後，這裡應該立刻出現一筆 `new_order / ok=true`。

### C. 重推會不會誤觸發（重要）
下一張單，**不要接單**，等 3 分鐘 → 應該收到一張「🔁 補發通知」的橘色卡片。
接單之後就不會再補（因為條件是 `status='new'`）。

### D. webhook 三項
```
公休                → 完全沒反應 ✅
002                 → 完全沒反應 ✅
真的下一張單         → 回「已收到訂單 #XXX」✅
```

---

## 🔴 另外還有一件更急的事：金鑰輪替

`laolu-line-keys.txt` 曾公開在網路上（07-27 事故），**8/1 前必須全部換掉**：

1. **BOSS_BIND_CODE**（最急：任何人傳一句話就變老闆）
2. 先查 `select line_user_id, created_at from line_admins order by created_at;` 有沒有陌生人
3. LINE Channel access token 重新發行
4. Channel secret 重新發行
5. WEBHOOK_SECRET 換新 → ⚠️ 換完要**重跑 `setup-secret-vault.sql`**（保險櫃裡的碼也要更新），否則門鈴會 403

📌 新金鑰一律存 `D:\laolu-secrets\`，**不要放回專案資料夾**。
