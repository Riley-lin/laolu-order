# 🚨 應急模式部署卡（2026-07-30 施工完成）

> **它解決什麼**：現在點餐入口鎖死在 LINE ——客人要從官方帳號進來、送出時還要以自己身分
> 傳一則訊息。平常這是優點（每張單都對得到一個真人），但 **LINE 平台一掛（07-28 發生過），
> 整間店的線上訂餐直接歸零**。這個模式是那時候的備援通道。

---

## 📋 妳要做的（兩步，5 分鐘）

### ① 跑 SQL

```
Supabase → SQL Editor → 貼 setup-emergency.sql 整份 → Run
```

**它會做**：
- `app_config` 加 `emergency_on`（開關）**＋把它加進 RLS 讀取白名單**
  （🔴 第一版漏了白名單這段——資料寫進去了但前端讀不到，**靜默失效、不報錯**。
   若妳在 07-30 深夜已經跑過一次，請**重跑一次**這份，只跑 SQL 就好，不用再部署 place-order）
- `app_secrets` 加 `emergency_code`（通行碼，**前端讀不到**）
- `orders` 加 `emergency` 欄位（⚡ 應急單標記）
- 三支只有登入者能呼叫的函式：`emergency_status` / `set_emergency` / `rotate_emergency_code`

> 💡 **為什麼開關和通行碼要分開放**：客人端必須知道「現在是不是應急模式」才能決定要不要
> 走 LINE，所以開關必須公開。但通行碼一旦公開就等於沒鎖 —— 它只能待在前端讀不到的保險櫃裡，
> 由伺服器端比對。

### ② 部署 `place-order`

```
Supabase → Edge Functions → place-order → 貼 index.ts → Deploy
```

前端（`index.html`、`boss.html`）細菌已經推上去了。

---

## 🎮 怎麼用（給老闆的）

### 平常：關著

什麼都不用管。客人一律從 LINE 點餐。

### LINE 掛掉時：三步驟

```
① 店務台 → 🚪 營業設定 → 滑到最下面「🚨 應急模式」→ 按【啟動】
② 按【📋 複製網址】
③ 把網址貼給客人（LINE 群組、簡訊、寫在店門口小黑板都行）
```

客人打開那個網址就能直接點餐，**不需要 LINE**。

### LINE 恢復了：記得關掉

```
店務台 → 營業設定 → 🟢 關閉應急模式
```

關掉之後就回到「每張單都找得到人」的狀態。

---

## ⚠️ 應急模式期間，有三件事不一樣

| 平常 | 應急模式 |
|---|---|
| 客人送單時會傳訊息到你的聊天室 | **不會傳**（LINE 掛了本來就傳不出去） |
| 找不到人可以在 LINE 上直接問他 | **只能打電話**（訂單會標 ⚡ 提醒你） |
| 黑名單擋得住棄單過的客人 | **擋不住**（沒有 LINE 身分可以比對） |

**所以看單台會在那張單上標紅：**

> ⚡ 應急單 · 客人沒有 LINE 身分，有事請直接電聯

客人端也會在最上面掛一條說明，告訴他「送出後請記下取餐編號，用下方查詢我的訂單看進度」——
不講清楚的話，他沒收到 LINE 回覆會以為沒送出去，然後重複下單。

---

## 🔒 安全設計（三道限制）

| 限制 | 作用 |
|---|---|
| ① **平常開關是關的** | 就算網址被外流，沒開就是死的 |
| ② **通行碼可一鍵換新** | 按【🔄 換新碼】→ 舊網址立刻失效 |
| ③ **只在故障那幾小時開** | 暴露時間極短 |

> ⚠️ **誠實說明**：帶碼的網址被轉發，對方也能開 —— **網址本身就是鑰匙**。
> 這不是完美的安全，是「故障時還能做生意」與「風險可控」之間的取捨。
> 真的被亂轉發，按【換新碼】就好。

**限流還在**：應急模式下沒有 LINE 身分可以認人，但 **IP 限流照樣運作**（10 分鐘 5 張）。
所以擋得住洪水，擋不住特定人 —— 這是已知的代價。

---

## 🧪 驗收（3 分鐘）

| # | 測什麼 | 通過的樣子 |
|---|---|---|
| **A** | **不開**應急模式，用 `?e=隨便打` 開首頁下單 | 被擋（跳「請從 LINE 進入」）|
| **B** | 開啟後，用店務台複製的網址（電腦瀏覽器也行）下單 | 成立，頁面最上面有 ⚡ 橫幅 |
| **C** | 看單台看那張單 | 標著 **⚡ 應急單 · 客人沒有 LINE 身分** |
| **D** | 用「查詢我的訂單」打姓名＋電話 | 查得到（走的是 `get_customer_orders`，不靠 LINE 身分）|
| **E** | 按【🔄 換新碼】後，用舊網址再下單 | 被擋 |
| **F** | 關閉應急模式後，用剛才的網址下單 | 被擋 |

**A 和 F 是最重要的兩項** —— 它們證明「開關真的有用」。如果沒開也能下單，那這個模式就是一直敞開的後門。

---

## 📌 已知邊界（不是 bug，是設計）

1. **應急單沒有 LINE 身分** → 黑名單失效、無法在 LINE 上聯絡客人
2. **帶碼網址等於鑰匙** → 被轉發就能用（用換新碼處理）
3. **關掉之後舊網址立刻失效** → 正在點餐的客人送出時會被擋，要重新給他網址
   （所以關之前先確認沒有人正在點）
