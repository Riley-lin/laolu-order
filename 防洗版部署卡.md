# 🛡️ 防洗版 ＋ 高額警示 ＋ 棄單黑名單 · 部署卡

> 2026-07-30 夜間施工完成，**尚未上線**。
> 規格來源：07-28 Riley 全數拍板（見 `待處理.md` 主工程段）

---

## 🔴 施工順序不能顛倒（很重要）

```
① SQL          ← 先建欄位與資料表
② place-order  ← 新版會用到那些欄位
③ index.html   ← 新版客人端會呼叫新版 place-order（細菌部署）
④ boss.html    ← 看單台的棄單按鈕（細菌部署）
```

**為什麼不能先部署前端**：新版客人端送單前會先呼叫「預檢」，
舊版 `place-order` 不認識這個請求 → 會回錯誤 → **全部客人都下不了單**。
所以前端要等後端上線之後才能推。細菌會等妳做完 ①② 再部署 ③④。

---

## ① 跑 SQL

```
Supabase → SQL Editor → 貼 setup-antiabuse.sql 整份 → Run
```

**它會做**：
- `order_throttle` 加 `line_user_id`、`total`（限流從「只認 IP」升級成「認人＋認金額」）
- `orders` 加 `no_show`（棄單）、`high_value`（高額標記）、`no_show_notify`（要不要通知客人）
- 新表 `blocklist`（封鎖名單，記到期時間 → 30 天自動失效，不需要排程）
- 新觸發器 `orders_no_show_notify`（老闆選「自動通知」時才發）
- 統計函式 `daily_summary()`（營業額與棄單損失分開算）

**跑完最下面會自動體檢**，四列都要有東西。

---

## ② 部署 `place-order`

```
Supabase → Edge Functions → place-order → 貼 supabase/functions/place-order/index.ts → Deploy
```

**這次的重點是「身分驗章」**——以前訂單裡的 `line_user_id` 是前端自己寫的，
後端只能相信；任何人都能冒名下單，**黑名單會是紙糊的**。
現在改成拿 LINE 簽發的 ID Token 去跟 LINE 對章，用驗過的身分覆蓋前端送來的。

> LINE 官方：`POST https://api.line.me/oauth2/v2.1/verify`（帶 id_token ＋ client_id），
> 回應的 `sub` ＝ "the User ID for which the ID token is generated"
> <https://developers.line.biz/en/docs/line-login/verify-id-token/>

---

## ③ 部署 `line-push`（棄單通知客人）

```
Supabase → Edge Functions → line-push → 貼 supabase/functions/line-push/index.ts → Deploy
```

只多了一段 `no_show` 通知。**刻意不受 `CUSTOMER_PUSH_ENABLED` 管**——
那個開關關掉的是「取餐進度」這種客人自己查得到的通知；
棄單通知客人不會主動去看，而且是老闆一張一張手動選才發的，額度可控。

---

## ④⑤ 前端（細菌部署，妳不用動）

`index.html`：ID Token、送單前預檢、被封鎖的話術＋一鍵撥電話
`boss.html`：🚫 標記棄單按鈕、⚠️ 高額標記、日報的棄單損失

---

# 📋 這次做了什麼（給老闆看的白話版）

## 1️⃣ 防洗版

| 規則 | 條件 | 動作 |
|---|---|---|
| 一般限流 | 同一人 **或** 同一 IP，10 分鐘 5 張 | 擋下 |
| 爆量警示 | 那 5 張是 **2 分鐘內**下完的 | 擋下 ＋ LINE 通知老闆「疑似洗版」 |

擋下時客人看到（Riley 定稿）：
> 下單太頻繁，老闆會忙不過來，請稍等幾分鐘 ，如有多單需求，請跟店家聯繫喔!📞0939-955-888

**為什麼要人＋IP 雙軌**：只認 IP 的話，換個網路（4G 切 WiFi）就繞過了；
只認人的話，有人用一堆假身分灌單就擋不住。兩條各補一個洞。

## 2️⃣ 高額連續下單警示（提醒，不擋）

- 條件：同一人，**30 分鐘內 2 張、且每張都超過 $500**
- 看單台那張單會標 ⚠️（免費）
- LINE 通知老闆「同一位客人 8 分鐘前下單 $650，本次 $720，請留意」

> 為什麼要「每張都超過」而不是「加起來超過」：一群同事各訂各的午餐很容易加總破 500，
> 那樣警示會天天響，變成狼來了就沒人看了。

📌 老闆嫌吵 → `place-order` 第 46 行 `HIGH_VALUE_PUSH = false`，
看單台的 ⚠️ 標記照留，只是不推播。

## 3️⃣ 棄單註記＋黑名單

**在哪按**：看單台 →「已完成」的訂單 → 🚫 標記棄單

**按下去會問「要怎麼告知客人」**，三選一：
| 選項 | 說明 |
|---|---|
| 📩 系統自動通知他 | 扣 1 則額度，語氣客氣、附申訴管道 |
| 📋 複製訊息，我自己貼 | 不扣額度，複製好貼進聊天室 |
| 🤐 先不通知 | 只做標記與暫停 |

**做了三件事**：
1. 這張單**不計入營業額**，改列「棄單損失」（日報會多一列）
2. 這位客人**線上訂餐暫停 30 天**（現場購買不受影響）
3. 他再來下單會看到：「您有未取餐紀錄，線上訂餐暫停至 X/XX…」＋ 一鍵撥電話

**按錯了怎麼辦**：同一張單會變成「↩️ 取消棄單註記」，按下去就還原，封鎖也一起解除。

> ⚠️ **棄單和取消是兩顆分開的按鈕**（Riley 07-27 裁示）：
> 取消 ＝ 老闆知情（缺料、客人來電說不要了）→ 不該怪客人
> 棄單 ＝ 做好了沒人領 → 有實際損失
> 混在一起的話，日報會分不清「沒做」跟「做了賠掉」，也會冤枉臨時取消的人。

---

# 🧪 驗收清單

| # | 測什麼 | 通過的樣子 |
|---|---|---|
| **A** | 正常下一張單 | 一切照舊，順利成立 |
| **B** | 連續下 6 張（10 分鐘內） | 第 6 張被擋，看到 Riley 定稿那句話 |
| **C** | 2 分鐘內連下 5 張 | 被擋 ＋ 老闆收到「⚠️ 疑似洗版」 |
| **D** | 30 分鐘內連下 2 張都 >$500 | 訂單成立，但看單台標 ⚠️ ＋ 老闆收到高額提醒 |
| **E** | 已完成的單按 🚫 標記棄單 → 選「先不通知」 | 卡片變 🚫 棄單、日報出現「棄單損失」 |
| **F** | 被標記的那個 LINE 帳號再下單 | 看到「線上訂餐暫停至 X/XX」＋ 問要不要打電話 |
| **G** | 按「↩️ 取消棄單註記」 | 還原、可以正常下單 |

> 💡 **B 測完記得清乾淨**：那 6 張測試單要在看單台取消掉，不然會污染日報。
> 限流紀錄 10 分鐘後自動過期，不用手動清。

---

# ⚠️ 一個要先知道的副作用

驗身分之後，**ID Token 過期的人會被擋**（頁面在 LINE 裡開太久沒動）。
訊息已經寫得很白：「若你已經在 LINE 裡面，請把這頁關掉重新點一次『我要點餐』」。

這是必要的代價 —— 沒有驗章，黑名單和限流都可以被一行程式繞過。
