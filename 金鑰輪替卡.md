# 🔑 金鑰輪替操作卡（2026-07-29 起，未完成）

> **起因**：`laolu-line-keys.txt` 於 2026-07-26 21:00 ～ 07-27 21:30 曾公開在
> `https://laolusian.pages.dev/laolu-line-keys.txt` 可直接下載（wrangler 部署不看 `.gitignore`）。
> 已止血，但**外洩已成事實 → 四把鑰匙必須全換**。
>
> 🔐 **新金鑰的實際值在 `D:\laolu-secrets\新金鑰_20260729.txt`**
> （這份卡刻意不寫值——它會進 GitHub，而 repo 是公開的）

---

## 📊 進度

| 步驟 | 狀態 |
|---|---|
| 0️⃣ 暫停接單 | ⬜ 開工前再按（07-29 那次已自動失效） |
| 1️⃣ 查 `line_admins` 有沒有陌生人 | ✅ **2 筆＝Riley＋老闆，乾淨，密語沒被人用過** |
| 2️⃣ LINE 後台重發 token ＋ secret | ⬜ **← 從這裡繼續** |
| 3️⃣ Supabase 一次改 4 個 Secret | ⬜ |
| 4️⃣ 跑 SQL 同步保險櫃 | ⬜ |
| 5️⃣ 部署 `line-webhook` ＋ `line-push` | ⬜ |
| 6️⃣ 驗收 ＋ 恢復接單 ＋ 通知老闆新密語 | ⬜ |

> ✅ **好消息：目前系統完全正常。** 停在第 2 步之前＝還沒動任何一把鑰匙，
> 舊 token 還活著，客人照樣點餐、老闆照樣收通知。**不是做一半卡住的狀態。**

---

## ⚠️ 動手前一定要知道

LINE 官方文件明載：

> "Reissuing a long-lived channel access token will invalidate the currently active long-lived channel access token."
> （重新發行 long-lived token → **目前這把立刻失效**）

📌 來源：<https://developers.line.biz/en/docs/basics/channel-access-token/>

**所以從第 2 步按下 Reissue 到第 5 步部署完成的這幾分鐘，推播是斷的。**
→ 開工前先 **店務台 → 🚪 營業設定 → ⏸️ 暫停接單**
→ 這幾步**不要中斷**，一路做到部署完成（合併操作後約 3 分鐘停機）

---

## 0️⃣ 暫停接單

```
店務台 → 🚪 營業設定 → ⏸️ 暫停接單
```
（暫停只存當日日期，隔天自動恢復；所以 07-29 那次早就失效了，要重按）

---

## 1️⃣ ✅ 已完成：查陌生人

```sql
select line_user_id as 用戶編號,
       created_at at time zone 'Asia/Taipei' as 綁定時間
from line_admins order by created_at;
```
結果 **2 筆**＝Riley＋老闆 → **沒有陌生人，外洩的密語沒被撿去用過**。
（分不出哪個是自己 → 傳 `id` 給官方帳號，它會回你的用戶編號）

---

## 2️⃣ LINE 後台重發兩把（← 從這裡繼續）

網址：<https://developers.line.biz/console/>

### ⓐ Channel access token
```
老滷仙 Provider → Messaging API Channel
→ 頁籤「Messaging API」→ 滑到最下面
→ Channel access token (long-lived) → Reissue
→ 問要保留舊 token 幾小時 → 選【0 hours】
```
> 💡 選 0 的理由：舊的**已經外洩了**，多留一小時就是多給人家一小時。

### ⓑ Channel secret
```
→ 頁籤「Basic settings」→ Channel secret → Issue / Reissue
```

📝 兩個值都貼進 `D:\laolu-secrets\新金鑰_20260729.txt` 的 ③ ④ 格。

---

## 3️⃣ Supabase 一次改 4 個 Secret

```
Supabase → Edge Functions → Secrets（或 Project Settings → Edge Functions）
```

| Secret 名稱 | 換成 |
|---|---|
| `BOSS_BIND_CODE` | 新密語（12 碼，避開 0/O/1/l/i 好打） |
| `WEBHOOK_SECRET` | 新的 40 碼 |
| `LINE_CHANNEL_ACCESS_TOKEN` | 第 2ⓐ 步拿到的 |
| `LINE_CHANNEL_SECRET` | 第 2ⓑ 步拿到的 |

> ⚠️ 貼上時**檢查頭尾有沒有多空白或換行**——M1 那晚就是被一個隱形字元卡成 403。

---

## 4️⃣ 跑 SQL 同步保險櫃（這步最容易漏）

`WEBHOOK_SECRET` **存在兩個地方**，只改一邊 → 資料庫門鈴會 403、訂單通知全斷。

```
Supabase → SQL Editor → 貼下面 → 把 <新的WEBHOOK_SECRET> 換成真值 → Run
```

```sql
insert into public.app_secrets (name, value)
values ('webhook_secret', trim(both e' \n\r\t' from '<新的WEBHOOK_SECRET>'))
on conflict (name) do update set value = excluded.value;

-- 確認存進去了（應該回 true）
select value = '<新的WEBHOOK_SECRET>' as 對得上嗎 from public.app_secrets
 where name = 'webhook_secret';
```

> 💡 為什麼有保險櫃：資料庫的門鈴（trigger）沒辦法讀 Edge Function 的 Secret，
> 所以通行碼要另外存一份在 `app_secrets` 表裡（RLS 上鎖，前端讀不到）。

---

## 5️⃣ 部署兩支程式

```
Edge Functions → line-webhook → 貼 supabase/functions/line-webhook/index.ts → Deploy
Edge Functions → line-push    → 貼 supabase/functions/line-push/index.ts    → Deploy
```
（程式碼**不用改**，這步只是讓函式重新啟動、抓到新的 Secret）

---

## 6️⃣ 驗收（四項，5 分鐘）

| # | 做什麼 | 通過的樣子 |
|---|---|---|
| **A** | 用**舊密語**傳「老闆綁定 <舊密語>」（值在 secrets 檔） | **完全沒反應**（舊的死了）|
| **B** | 用**新密語**傳「老闆綁定 <新密語>」 | 回「✅ 綁定成功」|
| **C** | 恢復接單 → 下一張測試單 | 老闆 LINE 收到 🔔 新訂單卡片 |
| **D** | 查推播紀錄 | 出現一筆 `new_order / ok=true / 200` |

```sql
select order_no, kind, ok, status_code,
       created_at at time zone 'Asia/Taipei' as 台北時間
from public.push_log order by created_at desc limit 5;
```

**C 沒過** → 十之八九是第 4 步的保險櫃沒同步，回去重跑一次。

---

## 7️⃣ 收尾

- [ ] 店務台 → ▶️ 恢復接單
- [ ] **通知老闆＆弟弟新密語**，請他們重新傳「老闆綁定 <新密語>」
      （舊的綁定紀錄還在 `line_admins`，理論上不用重綁也收得到通知；
       但要確認他們手上那組舊密語作廢了）
- [ ] `laolu-line-keys.txt` 改名成 `laolu-line-keys_舊_已作廢.txt`
- [ ] 📌 新金鑰永遠只放 `D:\laolu-secrets\`
